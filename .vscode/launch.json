{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "dbt_shop",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "envFile": "${workspaceFolder}/.vscode/.env",
      "console": "integratedTerminal"
    }
  ]
}